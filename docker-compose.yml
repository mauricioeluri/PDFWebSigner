version: "3.9"
name: assinador
services:
  service:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 80:80
    volumes:
      - ./app:/var/www/html
    working_dir: /var/www/html